spring:
  application:
    name: REST-APIS
  
  datasource:
    driver-class: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/training
    username: postgres
    password: postgres
 
  jpa :  
    hibernate:
      ddl-auto: update
    show-sql: true

#sql:
#   init:
#     mode: always